import React, { FC } from 'react';

interface LoginPageProps {}

const LoginPage: FC<LoginPageProps> = () => {
   return (
   <div>LOGIN to the workout! Username: Password:
   </div>
   );
};

export default LoginPage;